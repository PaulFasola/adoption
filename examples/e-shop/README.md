# E-Shop Adoption example

## Installation
This package can be installed via `yarn` or `npm`:  

```sh
$ yarn && yarn start

# or

$ npm i && npm start
```

## Featured Components
- PaymentRequest
- TransactionStatus