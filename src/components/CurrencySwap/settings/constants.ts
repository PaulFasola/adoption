// CURRENCYSWAP

/**
 * Cryptos have various decimal places. We stop displaying starting from the 8th digit by default.
 * Used by toLocaleString, maximumFractionDigits option.
 * See https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/toLocaleString
 */
export const DEFAULT_MAX_FRACTION_DIGITS = 8;

// CURRENCYSWAP - SETTINGS

/**
 * When input debouncing is enabled, this is the delay before
 */
export const DEFAULT_DEBOUNCE_DELAY = 500;
